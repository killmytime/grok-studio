# Grok Studio — 只需这些就能跑。供应商/模型/能力存在 SQLite，在设置里改。

# Grok 基本盘（第一次启动会写进 SQLite；之后可只在界面改）
GROK_BASE_URL=
GROK_API_KEY=

# 可选：整站访问密码。设了之后 IPv4 / IPv6 / localhost / 局域网同一道门。
# 不设则谁能连上端口谁就能用。不做多用户。
STUDIO_PASSWORD=

# 数据目录
DATA_DIR=./data
